# Spring Profile: Docker
spring.profiles.active=docker

# Datasource (Railway MySQL public URL)
spring.datasource.url=jdbc:mysql://hopper.proxy.rlwy.net:54156/railway?useSSL=false&allowPublicKeyRetrieval=true&autoReconnect=true
spring.datasource.username=root
spring.datasource.password=iufOxnNGCyoFkhZTuBpzpBSqBVKgmzEO
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server port (matches Dockerfile EXPOSE)
server.port=8082

# Management endpoints (optional, for health check)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
